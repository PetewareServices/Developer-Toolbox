version: 1.18.0
release date: 2026-4-24
notes: now 100% open sourced, completely undetected excluding features within the toolbox such as external tools and FPS-Boosters, etc. added and modified library methods to be more easier and better to use. completely reworked addon system with the new ui library changes so it updates on the ui when applying changes rather then having to reload the toolbox like it used to be. improved and fixed a few bugs regarding to error handling for executors with compatibility issues. improved the overall handling of the developer toolbox. renamed all aliases such as Dev Toolbox, Toolbox to Developer Toolbox. Fixed UTF8 Formatting issue with the arrows in the Instance Scanner feature. removed unnecessary yields / waits which improved the toolbox execution speed by like 10x. removed old ui cleanup on original source code and added to the wizard library src. removed references for the library object with gethui and just made the Library return the Object as well as the NewWindow method for a cleaner reference to the library's screen gui object. little bytecode optimisations and determinations, removed silly pcall checks to help with runtime optimisation. removing error handling on things such as addons since they are unnecessary and i dont know why anyone is deleting the addons through workspace while its running rather then just using the addons manager to do that. added fallback compatibility for fireproximityprompt and loadfile since they were easy. changed the UI handler on removed connection to use the direct object instead with the Destroying connection for better handling. added compatibility check cache to prevent multiple compatibility checks in the same session for optimisation.