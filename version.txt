version: 1.19.0
release date: 2026-4-24
notes: added configurations so it automatically saves the toggles status with writefile, uses denary for optimisation. added themes for the wizard library which i will make a documentation on it soon within this repo so people know how to use it. improved compatibility checks so it now checks for roblox place capabilities. added a settings table on top of main.lua so it takes in arguments from the function call returned from loadstring, when using the loadstring from the README. changed urls to use the Owner variable in settings rather then a fixed string so forking is easier. more redundant checks and better handling to prevent runtime errors related to compatibility issues with executors. fixed the queue on teleport issue (attempt to call a nil value error) added some optimisation improvements such as bytecode changes, using locals for fixed things such as local raw_args and local bell_ring_asset, etc. removed some checks that prevent runtime errors as they arent necessary which also improved optimisation performance.
